import React from "react"; // Import React library
import Layout from "./layouts/Layout"; // Import the main layout component

// Main App component
export default function App() {
  // The App component renders the Layout component
  // Layout usually contains the main structure of the app:
  // header, footer, sidebar, and main content
  return <Layout />;
}
